# postgres_exporter credentials (compose interpolation from host .env.prod).
# Role is provisioned with GRANT pg_monitor (not superuser). Key names only.
PROD___POSTGRES_EXPORTER__USERNAME=postgres_exporter
PROD___POSTGRES_EXPORTER__PASSWORD=change_me_postgres_exporter_password

# Caddy container env only (materialized to .env.caddy.prod from .env.prod).
# Key names only — never commit real secret values.
API_DOMAIN=api.example.com
WEB_DOMAIN=www.example.com
# Company/product surfaces and the apex redirect (#1605). Optional: while unset each vhost stays on
# its unreachable `.localhost` default. Never emit an empty value - see materialize_service_env.sh.
#
# These three carry the real hosts, not `example.com` placeholders. COMPANY_DOMAIN and
# PRODUCT_DOMAIN are pinned by `deploy/env/env.contract.json` allowedValues (the front compiles the
# surface canonical table in, so no other host can be served correctly), and APEX_DOMAIN only means
# anything as that pair's apex. A placeholder here reads as a value an operator may substitute,
# which is exactly the config the contract rejects.
COMPANY_DOMAIN=www.aquilaxk.site
PRODUCT_DOMAIN=easysubway.aquilaxk.site
APEX_DOMAIN=aquilaxk.site
MONITOR_DOMAIN=status.example.com
GRAFANA_DOMAIN=grafana.example.com
PROMETHEUS_DOMAIN=prometheus.example.com
CADDY_EMAIL=you@example.com
# Space-separated admin UI origins allowed to iframe Grafana/Uptime (CSP frame-ancestors).
ADMIN_EMBED_ORIGINS=https://www.example.com https://example.com
PROMETHEUS_BASIC_AUTH_USER=NEED_TO_SET_PROMETHEUS_BASIC_AUTH_USER
PROMETHEUS_BASIC_AUTH_HASH=NEED_TO_SET_PROMETHEUS_BASIC_AUTH_HASH
# Optional blue/green upstream overrides (defaults in Caddyfile)
# ADMIN_API_UPSTREAM=back_blue
# READ_API_UPSTREAM=back_blue
# WEB_UPSTREAM=front_blue
